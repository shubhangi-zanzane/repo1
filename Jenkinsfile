pipeline{
    agent{
        label 'built-in'
        customWorkspace '/data/pipeline'
    }
    stages{
        stage('node-1')
        {
            steps{
                echo "hello world"
            }
        }
    }
}
